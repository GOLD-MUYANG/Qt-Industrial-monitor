# 工业监控上位机系统设计

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | Industrial Monitoring System |
| 设计日期 | 2026-08-23 |
| 目标平台 | Ubuntu / WSL |
| UI 技术 | Qt 6 Widgets |
| 首版协议 | Modbus TCP |
| 项目定位 | 求职作品与面试演示项目 |
| 架构选择 | 动态协议插件 + 多线程数据流水线 |

本文档定义首版系统的功能范围、模块边界、线程模型、数据流、异常处理和验收标准。后续实现应以本文档为边界，不在首版中临时增加协议或业务功能。

## 2. 项目目标

本项目实现一套可以在单机上完整演示的工业设备监控系统，由两个独立程序和一个动态协议插件组成：

```text
VirtualPLC
    |
    | Modbus TCP
    v
IndustrialMonitor
    |
    +-- 实时展示
    +-- 状态统计
    +-- 阈值报警
    +-- SQLite 存储
    +-- 历史查询与 CSV 导出
```

项目重点不是模拟大型 SCADA 平台，而是完成一条可运行、可测试、可解释的工业数据链路：

```text
发现协议插件 -> 建立连接 -> 周期采集 -> 数据解析
-> 数据质量判断 -> 实时统计 -> 报警判断 -> SQLite 持久化
-> 实时界面与历史查询 -> 断线重连
```

完成后，演示者应当能够从源码解释每个数据对象由谁创建、在哪个线程使用、如何跨模块传递，以及失败时如何恢复。

## 3. 首版范围

### 3.1 必须实现

- `IndustrialMonitor` Qt Widgets 上位机。
- `VirtualPLC` Qt Widgets 模拟设备。
- 使用 `QPluginLoader` 动态加载 Modbus TCP 协议插件。
- 使用 `QModbusTcpClient` 异步读取和写入寄存器。
- 使用 `QModbusTcpServer` 模拟 PLC。
- 采集温度、压力、转速、电压和运行状态。
- 默认每 500 ms 采集一次，且同一设备最多只有一个读请求在途。
- 展示当前值、最近 60 秒最大值、最小值和平均值。
- 展示最近 60 秒实时曲线，支持暂停和恢复显示。
- 实现阈值报警、报警确认、报警恢复和历史查询。
- 实现连接状态机、超时、错误计数和自动重连。
- 使用 SQLite 保存设备、测点、测量值、报警规则和报警事件。
- 支持按设备、测点和时间范围查询历史数据。
- 支持 CSV 导出。
- 记录逻辑层通信日志，包括功能码、地址、数量、耗时和错误。
- VirtualPLC 支持自动变化、制造高温报警、断开通信和恢复通信。
- 使用 CTest 组织单元测试和集成测试。

### 3.2 明确不做

- Modbus RTU、普通 TCP、UDP 和裸串口通信。
- 协议插件运行时热卸载或无停机升级。
- 完整 Modbus TCP MBAP 报文抓包和原始 HEX 调试器。
- 配方管理、用户权限、远程部署和多机集群。
- PLC 厂商私有协议和真实硬件认证。
- 脚本系统、插件市场和通用工作流引擎。
- 为了展示技术而引入额外线程、消息中间件或依赖注入框架。

以上能力可作为后续扩展，但不能阻塞首版闭环。

## 4. 技术选择

| 能力 | 技术选择 | 选择原因 |
| --- | --- | --- |
| 桌面界面 | Qt Widgets | 与当前学习目标和桌面上位机场景一致 |
| 构建系统 | CMake 3.22+ | 当前环境可用，便于组织多个目标与测试 |
| Modbus | Qt Serial Bus | 提供异步 Client、Server、超时和重试机制 |
| 动态插件 | Qt Plugin System | 原生支持接口声明、元数据和运行时加载 |
| 实时曲线 | Qt Charts | 兼容当前 Qt 6.2.4；不以更新版本 Qt Graphs 为首版前提 |
| 数据库 | Qt SQL + SQLite | 部署简单，适合本地历史数据和测试 |
| 并发 | QObject Worker + QThread | 保留事件循环，适合定时器、网络对象和 queued signal |
| 测试 | Qt Test + CTest | 可测试 QObject、信号、状态机和数据库逻辑 |
| 配置 | JSON + QStandardPaths | 配置可读，运行数据写入用户数据目录 |

当前环境已安装 Qt 6.2.4 的 Core、Widgets、Network 和 SQL，但尚未发现 Qt Serial Bus、Qt Serial Port 与 Qt Charts 开发模块。首版不需要 Serial Port；开始实现前必须补齐 Qt Serial Bus 和 Qt Charts，并通过一个最小 CMake 探测工程验证模块可用。

### 4.1 配置事实来源

- `config/*.example.json` 只保存可读的首启示例，不是运行期配置的第二份副本。
- 首次启动创建数据库后，将示例设备、测点和报警规则导入 SQLite。
- 导入成功后，`device`、`tag` 和 `alarm_rule` 表成为运行期唯一事实来源。
- 窗口尺寸、最近选择页面等纯 UI 偏好使用 `QSettings`，不写入业务表。
- 后续启动不重复覆盖用户已经修改的设备和报警配置。

## 5. 总体架构

### 5.1 运行时组成

```text
IndustrialMonitor 进程
|
+-- UI 主线程
|   +-- MainWindow / 页面
|   +-- RealtimeModel / AlarmTableModel / DeviceListModel
|   +-- DeviceManager
|   +-- DeviceSession 控制器
|   +-- PluginManager
|
+-- 每台在线设备一个通信线程
|   +-- ModbusTcpWorker
|   +-- QModbusTcpClient
|   +-- 采集 QTimer
|
+-- 一个数据处理线程
|   +-- DataPipeline
|   +-- StatisticsEngine
|   +-- AlarmEngine
|
+-- 一个数据库写线程
|   +-- StorageWorker
|   +-- SQLite 写连接
|
+-- 一个历史查询线程
    +-- HistoryWorker
    +-- SQLite 只读连接

动态库
|
+-- libprotocol_sdk.so
+-- plugins/libmodbus_tcp_plugin.so

VirtualPLC 进程
|
+-- 控制界面
+-- QModbusTcpServer
+-- SimulationEngine
+-- 故障注入控制
```

### 5.2 架构原则

1. UI 只展示模型和发送用户命令，不直接持有协议对象或数据库连接。
2. 协议插件只负责连接、协议读写、数据解码和通信错误，不执行报警或数据库逻辑。
3. 数据处理层只接收协议无关的测量数据，不依赖 `QModbusDataUnit`。
4. 数据库层不判断报警，不访问 UI 控件。
5. 跨线程只传递值对象，不跨线程直接调用 Worker 方法。
6. 每个 QObject 及其子对象只在所属线程中创建、使用和销毁。
7. 插件的动态库生命周期长于插件创建的全部 Worker。

## 6. 目录与文件边界

```text
Industrial_Monitoring/
├── CMakeLists.txt
├── cmake/
│   └── InstallLayout.cmake
├── config/
│   ├── devices.example.json
│   └── alarm_rules.example.json
├── src/
│   ├── protocol_sdk/
│   │   ├── include/industrial/protocol/
│   │   │   ├── IProtocolPlugin.h
│   │   │   ├── AbstractDeviceWorker.h
│   │   │   ├── ProtocolSdkExport.h
│   │   │   ├── ProtocolTypes.h
│   │   │   └── ProtocolVersion.h
│   │   └── ProtocolTypes.cpp
│   ├── plugins/
│   │   └── modbus_tcp/
│   │       ├── ModbusTcpPlugin.h/.cpp
│   │       ├── ModbusTcpWorker.h/.cpp
│   │       ├── ModbusRegisterCodec.h/.cpp
│   │       └── modbus_tcp.json
│   ├── monitor/
│   │   ├── main.cpp
│   │   ├── application/
│   │   │   ├── PluginManager.h/.cpp
│   │   │   ├── DeviceManager.h/.cpp
│   │   │   ├── DeviceSession.h/.cpp
│   │   │   └── ApplicationController.h/.cpp
│   │   ├── domain/
│   │   │   ├── Measurement.h
│   │   │   ├── TagDefinition.h
│   │   │   ├── DeviceStateMachine.h/.cpp
│   │   │   ├── StatisticsEngine.h/.cpp
│   │   │   └── AlarmEngine.h/.cpp
│   │   ├── data/
│   │   │   ├── DataPipeline.h/.cpp
│   │   │   ├── StorageWorker.h/.cpp
│   │   │   ├── HistoryWorker.h/.cpp
│   │   │   ├── DatabaseMigrator.h/.cpp
│   │   │   └── CsvExporter.h/.cpp
│   │   └── presentation/
│   │       ├── MainWindow.h/.cpp
│   │       ├── pages/
│   │       ├── models/
│   │       └── widgets/
│   └── virtual_plc/
│       ├── main.cpp
│       ├── VirtualPlcWindow.h/.cpp
│       ├── VirtualPlcServer.h/.cpp
│       ├── SimulationEngine.h/.cpp
│       └── RegisterBank.h/.cpp
├── tests/
│   ├── unit/
│   ├── integration/
│   └── fixtures/
└── 文档/
    └── 工业监控系统设计.md
```

### 6.1 关键职责限制

| 模块 | 负责 | 不负责 |
| --- | --- | --- |
| `protocol_sdk` | 插件 ABI、通用命令、数据和错误类型 | 具体协议、UI、数据库 |
| `modbus_tcp` | Modbus TCP 连接、轮询、写入、解码 | 报警、统计、SQLite |
| `PluginManager` | 扫描、验证、持有插件加载器 | 设备重连、业务数据处理 |
| `DeviceSession` | 单设备 Worker、线程和关闭顺序 | 解析寄存器、显示数据 |
| `DeviceManager` | 设备会话编排与状态汇总 | 直接访问 QModbusClient |
| `DataPipeline` | 数据质量、统计、报警的处理顺多序 | SQL 和界面控件 |
| `StorageWorker` | 数据库迁移、批量写入 | 历史页面模型、报警判断 |
| `HistoryWorker` | 只读查询与 CSV 数据流 | 实时写入 |
| `presentation` | 页面、模型和用户命令 | 协议、SQL、报警规则实现 |
| `virtual_plc` | 模拟寄存器和故障场景 | 复用上位机业务逻辑 |

## 7. 动态协议插件设计

### 7.1 插件接口

`IProtocolPlugin` 是不继承 QObject 的纯 C++ 工厂接口：

```cpp
class IProtocolPlugin
{
public:
    virtual ~IProtocolPlugin() = default;

    virtual ProtocolDescriptor descriptor() const = 0;
    virtual AbstractDeviceWorker* createWorker(
        const DeviceConfig& config) = 0;
};
```

接口 IID 固定为：

```text
com.muyang.IndustrialMonitor.ProtocolPlugin/1.0
```

插件类同时继承 `QObject` 和 `IProtocolPlugin`，使用 `Q_PLUGIN_METADATA` 和 `Q_INTERFACES` 导出。插件 JSON 至少包含：

```json
{
  "key": "modbus-tcp",
  "displayName": "Modbus TCP",
  "apiVersion": 1,
  "capabilities": ["read-registers", "write-register"]
}
```

### 7.2 Worker 接口

`AbstractDeviceWorker` 继承 QObject，并定义跨线程异步契约：

```cpp
class AbstractDeviceWorker : public QObject
{
    Q_OBJECT

public slots:
    virtual void start() = 0;
    virtual void stop() = 0;
    virtual void writeValue(const WriteRequest& request) = 0;

signals:
    void stateChanged(const DeviceState& state);
    void samplesReady(const SampleBatch& samples);
    void writeFinished(const WriteResult& result);
    void communicationError(const DeviceError& error);
    void transactionLogged(const TransactionLog& log);
    void stopped();
};
```

连接成功是异步事件，因此 `start()` 不返回表示连接结果的 `bool`。命令是否完成必须通过信号表达。

### 7.3 ABI 与加载约束

- `protocol_sdk` 使用 `Q_DECL_EXPORT` / `Q_DECL_IMPORT` 定义明确的共享库导出宏；主程序和插件链接同一个 SDK 目标。
- 主程序和插件必须使用同一编译器 ABI、Qt 主次版本和 `protocol_sdk` 版本构建。
- `PluginManager` 保存每个 `QPluginLoader`，不得使用局部加载器后丢失生命周期。
- 加载后必须检查 IID、`apiVersion`、插件 key 和 `qobject_cast` 结果。
- 单个插件失败只导致该协议不可用，不能阻止主程序启动。
- `createWorker()` 返回无 parent 的 Worker，由 `DeviceSession` 负责线程编排和最终销毁。
- 首版不支持运行时卸载。退出前先停止全部设备线程，再销毁插件对象和加载器。
- 插件目录固定为可执行文件同级的 `plugins/`，开发构建和安装构建使用相同相对布局。

## 8. 线程模型与生命周期

### 8.1 线程分工

| 线程 | 对象 | 说明 |
| --- | --- | --- |
| UI 主线程 | Widgets、Models、Manager、PluginManager | 仅执行轻量编排和显示更新 |
| 通信线程 | 单台设备的 Worker、QModbusTcpClient、QTimer | 每台在线设备一个线程 |
| 数据处理线程 | DataPipeline、StatisticsEngine、AlarmEngine | 所有设备共享，串行处理业务数据 |
| 数据库写线程 | StorageWorker、SQLite 写连接 | 使用事务批量写入 |
| 历史查询线程 | HistoryWorker、SQLite 只读连接 | 避免历史查询阻塞实时写入和 UI |

首版只有一台设备时仍保留以上边界，以真实展示插件设备线程、数据处理线程和数据库连接线程亲和性。不得继续拆出日志线程、告警线程或统计线程。

### 8.2 创建顺序

```text
ApplicationController 启动
-> 注册跨线程元类型
-> 启动 DataPipeline 线程
-> 启动 StorageWorker 线程
-> 启动 HistoryWorker 线程
-> PluginManager 扫描 plugins/
-> DeviceManager 根据配置创建设备会话
-> DeviceSession 创建 Worker 和 QThread
-> Worker moveToThread
-> QThread::started -> Worker::start
-> Worker 在通信线程创建 QModbusTcpClient 和 QTimer
```

Worker 在主线程创建时不能创建计时器和网络子对象。它移动到目标线程后，才在 `start()` 中创建这些对象。

### 8.3 跨线程数据

以下类型是只包含值语义的 DTO，使用 `Q_DECLARE_METATYPE` 并在启动时注册：

- `DeviceConfig`
- `DeviceState`
- `MeasurementSample`
- `SampleBatch`
- `WriteRequest`
- `WriteResult`
- `DeviceError`
- `TransactionLog`
- `AlarmEvent`
- `RealtimeSnapshot`

跨线程连接明确使用 `Qt::QueuedConnection`。接收方不得保存发送方对象指针，也不得从 UI 线程直接调用 Worker。

### 8.4 关闭顺序

```text
MainWindow 请求退出
-> ApplicationController 禁止新命令
-> DeviceManager 请求全部 Worker 停止轮询和断开连接
-> 等待 Worker::stopped
-> 退出并等待全部通信线程
-> DataPipeline 刷新待处理数据并退出
-> StorageWorker 提交剩余事务、关闭数据库并退出
-> HistoryWorker 取消查询、关闭只读连接并退出
-> 销毁 PluginManager 和 QPluginLoader
-> 关闭 UI
```

所有等待均设置有限超时。超时后记录错误并继续可控退出，不无限阻塞主线程。

## 9. Modbus TCP 数据设计

### 9.1 开发连接参数

| 参数 | 默认值 |
| --- | --- |
| 主机 | `127.0.0.1` |
| 端口 | `1502` |
| Server Address / Unit ID | `1` |
| 轮询周期 | `500 ms` |
| 单次请求超时 | `800 ms` |
| Qt 协议级重试 | `1` 次 |
| 连续失败阈值 | `3` 次 |

开发环境不默认使用端口 `502`，避免 Linux 低端口权限影响演示。

### 9.2 Holding Register 映射

| Qt 起始地址 | 工业表示 | 名称 | 编码 | 权限 |
| ---: | ---: | --- | --- | --- |
| 0 | 40001 | 温度 | 有符号整数，值除以 10，单位 ℃ | 读 |
| 1 | 40002 | 压力 | 无符号整数，值除以 100，单位 MPa | 读 |
| 2 | 40003 | 电机转速 | 无符号整数，单位 rpm | 读 |
| 3 | 40004 | 电压 | 无符号整数，值除以 10，单位 V | 读 |
| 4 | 40005 | 运行状态 | 位标志 | 读 |
| 10 | 40011 | 目标转速 | 无符号整数，单位 rpm | 写 |

`40001` 是面向工程人员的文档表示，传给 `QModbusDataUnit` 的地址是 `0`。该区别必须在代码常量和 README 中明确。

运行状态位定义：

```text
bit 0: Running
bit 1: Warning
bit 2: Fault
bit 3: Maintenance
```

### 9.3 轮询过程

每次轮询使用功能码 03，一次读取地址 0 开始的 5 个 Holding Registers：

```text
QTimer::timeout
-> 检查连接状态
-> 检查 inFlight 标志
-> sendReadRequest(HoldingRegisters, 0, 5, unitId)
-> 保存请求编号和开始时间
-> QModbusReply::finished
-> 检查协议错误
-> ModbusRegisterCodec 解码
-> 发出 SampleBatch 和 TransactionLog
-> reply->deleteLater()
-> 清除 inFlight
```

如果上一次请求尚未完成，本次定时器触发只记录一次 skipped poll，不创建新请求。这样不会因设备变慢造成请求队列无限增长。

写入目标转速使用功能码 06。写入结果必须通过 `writeFinished` 返回，UI 不能在发出请求时提前显示成功。

### 9.4 通信日志边界

首版记录：

- 时间戳和设备 ID。
- TX 或 RX 方向。
- 请求编号。
- 功能码。
- 寄存器类型、起始地址和数量。
- 解码值或异常码。
- 往返耗时。
- Qt 错误码和错误文本。

该日志是逻辑事务日志，不声称是网卡抓包结果，也不伪造 Qt API 未提供的完整 MBAP 原始字节。

## 10. 连接状态机与重连

### 10.1 状态定义

```text
Stopped
   |
   | start
   v
Connecting -- connected ----------------------> Online
   |                                             |
   | connection failed                          | three consecutive failures
   v                                             v
Reconnecting -- retry timer ----------------> Connecting

Stopped / Connecting / Online / Reconnecting
   |
   | stop
   v
Stopping -> Stopped

Stopped / Connecting
   |
   | unrecoverable local configuration error
   v
Faulted -- configuration corrected ----------> Stopped
```

`Faulted` 只用于不可恢复的本地配置或插件错误，例如插件版本不兼容、地址配置非法。网络断开不进入永久 Faulted，而是进入 Reconnecting。

### 10.2 错误分类

| 类型 | 示例 | 处理 |
| --- | --- | --- |
| 配置错误 | IP 非法、地址越界 | 进入 Faulted，等待用户修改 |
| 连接错误 | 拒绝连接、服务器关闭 | 进入 Reconnecting |
| 超时 | Reply 超时 | 计入连续失败，达到阈值后重连 |
| 协议错误 | Modbus exception | 记录异常；非法地址等确定性错误不盲目重试 |
| 数据错误 | 长度不符、编码越界 | 标记 Bad，不写入正常统计 |
| 插件错误 | 加载失败、ABI 不兼容 | 禁用对应协议并展示错误 |

### 10.3 重连策略

应用级重连使用退避间隔：

```text
1 s -> 2 s -> 4 s -> 8 s -> 10 s -> 10 s ...
```

- 连接成功并完成第一次有效采集后，退避计数清零。
- 用户主动断开会取消重连计时器。
- 同一设备只允许存在一个连接尝试。
- 重连期间保留最后一个有效值，但数据质量改为 `Stale`。
- 连续失败达到阈值时生成一条通信报警；恢复采集时恢复同一条报警，不重复创建大量记录。

## 11. 数据处理与实时统计

### 11.1 数据对象

```cpp
struct MeasurementSample
{
    QString deviceId;
    QString tagId;
    quint16 rawValue;
    double engineeringValue;
    DataQuality quality;
    QDateTime timestampUtc;
    quint64 sequence;
};
```

时间统一以 UTC 写入数据库，整数字段使用 Unix epoch 毫秒，UI 按本地时区显示。`DataQuality` 取值为 `Good`、`Stale` 或 `Bad`。

### 11.2 DataPipeline 顺序

```text
SampleBatch
-> 验证设备 ID、序号和时间戳
-> 验证测点值域
-> 更新最近 60 秒滚动窗口
-> 计算 current/min/max/average
-> AlarmEngine 处理阈值
-> 生成 RealtimeSnapshot
-> 投递数据库写入批次
```

Bad 数据保留到通信日志，但不参与统计和阈值报警。Stale 数据显示在 UI 中，但不作为新样本进入滚动统计。

### 11.3 实时窗口

- 每个数值测点保存最近 120 个有效样本，对应 500 ms 采集下的 60 秒。
- 最大值、最小值和平均值均针对该滚动窗口，不表示当天或全部历史。
- 暂停按钮只暂停曲线和数字模型的可视更新。
- 暂停期间采集、报警和存储继续运行。
- 恢复后直接显示最新 60 秒窗口，不逐帧回放暂停期间的数据。

## 12. 报警系统

### 12.1 默认规则

| 测点 | 下限 | 上限 | 严重级别 | 回差 |
| --- | ---: | ---: | --- | ---: |
| 温度 | 10 ℃ | 80 ℃ | Critical | 2 ℃ |
| 压力 | 0.5 MPa | 2.0 MPa | Warning | 0.1 MPa |
| 转速 | 500 rpm | 3000 rpm | Warning | 100 rpm |
| 电压 | 210 V | 240 V | Warning | 5 V |

通信断开使用独立的 Communication/Critical 规则，不依赖测量值。

### 12.2 报警生命周期

```text
Inactive
-> Pending
-> ActiveUnacknowledged
-> ActiveAcknowledged
-> RecoveredAcknowledged
```

如果报警在确认前恢复，则状态为 `RecoveredUnacknowledged`；用户确认后结束。

- 数值连续 3 个 Good 样本越界才激活，过滤单点毛刺。
- 恢复必须连续 3 个 Good 样本进入带回差的正常区间。
- 同一设备、同一规则在活动期间只保留一个报警实例。
- 确认只改变确认状态，不改变现场报警条件。
- 恢复由数据条件触发，不能由确认按钮伪造。
- 每次激活、确认和恢复都记录时间。

### 12.3 报警事件字段

- 报警 ID。
- 设备 ID 和测点 ID。
- 规则 ID。
- 报警类型和严重级别。
- 触发值、阈值和单位。
- 激活时间、确认时间、恢复时间。
- 确认状态和确认备注。

## 13. SQLite 设计

### 13.1 数据库位置与连接

数据库文件位于 `QStandardPaths::AppDataLocation`，不写入源码目录。数据库连接遵守以下规则：

- `StorageWorker` 在线程内部创建唯一写连接。
- `HistoryWorker` 在线程内部创建唯一只读连接。
- QSqlDatabase 连接不跨线程传递。
- 启用 `PRAGMA journal_mode=WAL` 和 `PRAGMA foreign_keys=ON`。
- 写入使用预编译语句和事务批处理。
- 连接在线程退出前关闭并移除。

### 13.2 表结构

```sql
CREATE TABLE device (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    protocol_key TEXT NOT NULL,
    host TEXT NOT NULL,
    port INTEGER NOT NULL,
    unit_id INTEGER NOT NULL,
    poll_interval_ms INTEGER NOT NULL,
    timeout_ms INTEGER NOT NULL,
    protocol_retries INTEGER NOT NULL,
    failure_threshold INTEGER NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE tag (
    id TEXT PRIMARY KEY,
    device_id TEXT NOT NULL,
    name TEXT NOT NULL,
    unit TEXT NOT NULL,
    register_type INTEGER NOT NULL,
    address INTEGER NOT NULL,
    scale REAL NOT NULL,
    writable INTEGER NOT NULL DEFAULT 0,
    FOREIGN KEY(device_id) REFERENCES device(id)
);

CREATE TABLE measurement (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id TEXT NOT NULL,
    tag_id TEXT NOT NULL,
    value REAL NOT NULL,
    quality INTEGER NOT NULL,
    timestamp_utc INTEGER NOT NULL,
    sequence INTEGER NOT NULL,
    FOREIGN KEY(device_id) REFERENCES device(id),
    FOREIGN KEY(tag_id) REFERENCES tag(id)
);

CREATE TABLE alarm_rule (
    id TEXT PRIMARY KEY,
    device_id TEXT NOT NULL,
    tag_id TEXT,
    rule_type INTEGER NOT NULL,
    lower_limit REAL,
    upper_limit REAL,
    hysteresis REAL NOT NULL DEFAULT 0,
    activation_samples INTEGER NOT NULL DEFAULT 3,
    recovery_samples INTEGER NOT NULL DEFAULT 3,
    severity INTEGER NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE alarm (
    id TEXT PRIMARY KEY,
    rule_id TEXT NOT NULL,
    device_id TEXT NOT NULL,
    tag_id TEXT,
    message TEXT NOT NULL,
    severity INTEGER NOT NULL,
    trigger_value REAL,
    activated_at_utc INTEGER NOT NULL,
    acknowledged_at_utc INTEGER,
    recovered_at_utc INTEGER,
    acknowledgement_note TEXT,
    FOREIGN KEY(rule_id) REFERENCES alarm_rule(id)
);
```

索引：

```sql
CREATE INDEX idx_measurement_device_tag_time
ON measurement(device_id, tag_id, timestamp_utc);

CREATE INDEX idx_alarm_device_time
ON alarm(device_id, activated_at_utc);
```

### 13.3 写入与保留策略

- 采集周期是 500 ms，默认每 1 秒为每个测点持久化一个最新 Good 样本。
- StorageWorker 每 1 秒或累计 50 行时提交一次事务，以先达到者为准。
- 报警激活、确认和恢复不降采样，立即进入写队列。
- 跨线程每次投递一个批次而不是逐行投递，减少事件队列压力。
- StorageWorker 保留最多 10000 行测量重试缓冲和 1000 条报警事件缓冲；达到上限时丢弃最旧测量值并产生明确的 StorageOverflow 错误，不静默增长内存。
- SQLite `busy_timeout` 默认 500 ms；写连接失败后每 2 秒尝试恢复，恢复前不阻塞 UI 或通信线程。
- 首版默认保留 7 天测量数据，启动后每天执行一次分批清理。
- 清理每批限制行数，避免长事务阻塞实时写入。
- 数据库写入失败不会覆盖实时响应；UI 显示独立的存储状态。

### 13.4 历史查询与 CSV

- 查询必须包含开始和结束时间，默认最近 1 小时，单次最大 24 小时。
- 查询按 `timestamp_utc` 排序并分页返回。
- 历史曲线最多渲染 5000 个点，超过时按时间桶降采样。
- CSV 使用 UTF-8，字段为本地时间、设备、测点、数值、单位和数据质量。
- 导出先写临时文件，成功后原子替换目标文件。
- 用户取消导出时删除临时文件，不留下半成品。

## 14. IndustrialMonitor 界面

### 14.1 导航结构

```text
MainWindow
├── 实时监控
├── 设备管理
├── 报警中心
├── 历史数据
└── 通信日志
```

页面通过共享模型接收数据，不直接订阅协议 Worker。

### 14.2 实时监控页

- 顶部显示设备选择、连接状态、连接/断开命令和暂停按钮。
- 显示温度、压力、转速、电压和运行状态。
- 每个数值测点显示当前值、最近 60 秒最小值、最大值和平均值。
- 曲线固定展示最近 60 秒，横轴使用时间，纵轴显示工程单位。
- 离线时保留最后值但显示 Stale；不能把旧值伪装成实时数据。
- 写入目标转速使用数值输入和明确的写入命令，结果异步反馈。

### 14.3 设备管理页

- 显示设备名称、协议、端点、Unit ID、状态和最近通信时间。
- 支持编辑 IP、端口、Unit ID、轮询周期、超时和启用状态。
- 协议下拉框来自 `PluginManager` 已加载的插件描述，不硬编码 Modbus TCP。
- 修改正在连接的设备配置时，先停止旧会话，再使用新配置重建会话。

### 14.4 报警中心

- 活动报警和历史报警使用两个标签页。
- 表格字段包括时间、设备、测点、消息、触发值、等级、确认状态和恢复状态。
- 支持确认单条报警并填写可选备注。
- Critical、Warning 和 Communication 使用颜色、图标和文字共同区分，不能只依赖颜色。

### 14.5 历史数据页

- 按设备、测点、开始时间和结束时间筛选。
- 查询结果同时提供曲线和明细表格。
- 支持分页、取消查询和 CSV 导出。
- 查询失败、无结果和取消是三个不同状态。

### 14.6 通信日志页

- 按设备、方向、成功状态和时间过滤。
- 显示功能码、地址、数量、耗时、结果摘要和错误。
- 日志模型保留最近 2000 条，避免 UI 内存无限增长。
- 文件日志使用滚动策略，与界面模型分离。

## 15. VirtualPLC 设计

### 15.1 模块边界

| 模块 | 职责 |
| --- | --- |
| `RegisterBank` | 保存寄存器值、范围和读写规则 |
| `SimulationEngine` | 产生可重复的温度、压力、转速和电压变化 |
| `VirtualPlcServer` | 配置 QModbusTcpServer 并同步寄存器 |
| `VirtualPlcWindow` | 展示值并发送模拟和故障命令 |
| `UiFont` | 加载内嵌 CJK 字体并设置应用全局 UI 字体 |

Server 保持在 VirtualPLC UI 线程，因为首版只有一个本地客户端且工作量很轻。不得为了与上位机对称而增加无必要的服务器线程。

### 15.2 正常模拟

- 每 500 ms 更新一次。
- 默认温度围绕 42 ℃ 缓慢变化。
- 默认压力围绕 1.2 MPa 缓慢变化。
- 实际转速逐步逼近目标转速，而不是瞬间跳变。
- 电压围绕 220 V 小幅变化。
- 使用固定随机种子或确定性函数，使测试和演示可复现。

### 15.3 故障注入

| 命令 | 行为 | 预期上位机表现 |
| --- | --- | --- |
| 制造高温报警 | 温度设置并保持在 86.3 ℃ | 3 个样本后产生 Critical 报警 |
| 恢复正常温度 | 温度恢复到约 42 ℃ | 满足回差和连续样本后报警恢复 |
| 断开通信 | 关闭 Modbus Server 监听和连接 | 上位机进入 Reconnecting 并产生通信报警 |
| 恢复通信 | 重新监听 1502 端口 | 上位机自动连接并恢复通信报警 |
| 冻结数据 | 停止值变化但继续响应 | 用于区分设备冻结与通信断开 |

VirtualPLC 界面只控制模拟器，不直接通知上位机制造何种结果；所有结果必须经过真实 Modbus TCP 链路出现。

## 16. 日志与可观测性

日志类别：

- `app.lifecycle`
- `plugin.loader`
- `device.session`
- `protocol.modbus`
- `data.pipeline`
- `alarm.engine`
- `storage.sqlite`
- `virtualplc.server`

每条日志至少包含时间、级别、类别和消息。设备相关日志增加 device ID，请求相关日志增加 request ID。文件日志按大小滚动，默认单文件 5 MB，保留 3 个文件。日志中不记录密码等敏感配置。

## 17. 错误呈现原则

- 通信失败、数据质量异常和数据库失败是相互独立的状态。
- SQLite 写入失败不能把已经采集到的实时数据改成通信失败。
- 4 类用户可见错误分别为：插件加载、设备通信、数据处理和存储。
- 状态栏只显示最近的重要事件；详细原因进入对应页面和日志。
- 可恢复错误提供重试状态，不使用阻塞式模态对话框反复打断监控。
- 配置错误必须指出具体字段，不只显示“连接失败”。

## 18. 测试设计

### 18.1 单元测试

| 测试文件 | 覆盖内容 |
| --- | --- |
| `tst_modbus_register_codec.cpp` | 缩放、符号、位标志和非法长度 |
| `tst_device_state_machine.cpp` | 全部状态转移、停止重连、失败阈值 |
| `tst_statistics_engine.cpp` | 滚动窗口、最值、平均值、Bad/Stale 排除 |
| `tst_alarm_engine.cpp` | 连续样本、回差、确认、恢复和去重 |
| `tst_plugin_manager.cpp` | 正常插件、错误 IID、错误 API 版本和缺失文件 |
| `tst_database_migrator.cpp` | 首次建库、重复迁移和失败回滚 |
| `tst_csv_exporter.cpp` | UTF-8、字段顺序、取消和原子输出 |

测试代码按职责拆分，不把全部测试堆进一个文件。

### 18.2 集成测试

- 加载真实 `libmodbus_tcp_plugin.so` 并创建设备 Worker。
- 使用无界面的 VirtualPLC 核心启动测试服务器，验证读取 5 个寄存器。
- 验证写入目标转速后 VirtualPLC 寄存器变化。
- 停止服务器，验证状态进入 Reconnecting 和通信报警只创建一次。
- 恢复服务器，验证自动重连、有效采集恢复和报警恢复。
- 连续采集后验证 SQLite 行数、时间戳、质量和事务提交。
- 并发执行实时写入和历史查询，验证 UI 事件循环不被阻塞。
- 验证退出时所有线程在限定时间内结束。

### 18.3 UI 与稳定性验证

- 使用 Qt offscreen 平台执行页面创建和模型绑定烟雾测试。
- 在桌面环境分别检查 1280×720、1440×900 和 1920×1080。
- 检查长设备名、长错误文本和无数据状态不会造成控件重叠。
- 自动执行 30 分钟循环断线与重连测试。
- 发布前执行一次至少 8 小时人工稳定性运行，记录内存、线程数、数据库大小和错误计数。

测试结果必须区分环境限制和产品回归。例如沙箱禁止监听端口时，不能把失败直接解释成 Modbus 逻辑错误。

## 19. 构建与部署

构建目标：

```text
protocol_sdk              shared library
modbus_tcp_plugin         module library
industrial_monitor        executable
virtual_plc               executable
industrial_unit_tests     test executables
industrial_integration_tests
```

安装布局：

```text
bin/
├── industrial_monitor
├── virtual_plc
├── libprotocol_sdk.so
└── plugins/
    └── libmodbus_tcp_plugin.so
```

CMake 配置阶段必须验证 Qt6::Core、Widgets、Network、Sql、SerialBus、Charts 和 Test。缺失模块时给出清晰错误，不允许构建到链接阶段才失败。

程序首次启动时从示例 JSON 初始化 SQLite 业务配置；数据库和日志写入 `QStandardPaths::AppDataLocation`，UI 偏好写入 `QSettings`。源码目录保持只读也应能正常运行。

## 20. 一个月实施边界

### 第一周：骨架、插件与模拟器

- [x] 建立 CMake 多目标工程和测试框架。
- [x] 完成 protocol_sdk、插件发现和加载失败测试。
- [x] 完成 VirtualPLC 寄存器映射与正常模拟。
- [x] 完成 Modbus TCP 插件的连接、读、写最小链路。

第一周结束标准：控制台测试能够通过动态插件从 VirtualPLC 读取 5 个寄存器。

#### 第一周完成记录（2026-08-23）

完成状态：**已完成并通过自动化验证**。

实际交付目标：

| 目标 | 产物 | 第一周职责 |
| --- | --- | --- |
| `protocol_sdk` | `build/lib/libprotocol_sdk.so` | 定义插件 IID、API 版本、DTO、异步 Worker 契约和元类型注册 |
| `modbus_tcp_plugin` | `build/bin/plugins/libmodbus_tcp_plugin.so` | 动态导出 `modbus-tcp`，连接后读取一次地址 0 至 4，并异步写地址 10 |
| `virtual_plc_core` | 静态内部库 | 保存寄存器、执行确定性 500 ms 模拟、封装 `QModbusTcpServer` |
| `virtual_plc` | `build/bin/virtual_plc` | 提供最小 Qt Widgets 模拟器界面，默认监听 `127.0.0.1:1502`、Unit ID 1 |
| `monitor_application` | 静态内部库 | 扫描插件目录，校验 IID、API 版本、key 和接口转换，并持有加载器 |
| `industrial_monitor` | `build/bin/industrial_monitor` | 第一周控制台验收入口；动态加载插件并输出 5 个工程值 |

本周按职责拆分的测试：

- `tst_protocol_types.cpp`：跨线程 DTO 元类型注册。
- `tst_register_bank.cpp`：默认寄存器映射、目标转速和非法地址。
- `tst_simulation_engine.cpp`：实际转速渐变与可重复模拟。
- `tst_modbus_register_codec.cpp`：5 个寄存器缩放、有符号温度和非法长度。
- `tst_plugin_manager.cpp`：目录缺失、空目录和真实错误 API 插件拒绝。
- `tst_virtual_plc_server.cpp`：真实 TCP Server 启停和重启。
- `tst_modbus_plugin_integration.cpp`：真实动态插件连接 VirtualPLC，读取 5 个寄存器并写入目标转速。
- `tst_virtual_plc_ui_font.cpp`：offscreen 创建真实窗口，验证最终 UI 字体包含中文字形。

验证命令与结果：

```text
cmake -S . -B build -DBUILD_TESTING=ON       -> 成功
cmake --build build -j2                      -> 成功
ctest --test-dir build --output-on-failure   -> 8/8 通过，0 失败
```

两个集成测试需要监听本地 TCP 端口；受限沙箱中端口申请会失败，本次结果来自允许环回网络的同一构建环境。当前 Ubuntu Qt 6.2.4 安装缺少 `canbusutil` 和 Qt6SerialPort 开发配置，但 SerialBus 头文件与共享库存在；`cmake/ResolveQtSerialBus.cmake` 对该不完整包执行显式验证和受限回退，完整 Qt 安装仍走官方 CMake 包。

中文方框字修复（2026-08-24）：源码与 locale 均为 UTF-8，但验证环境没有系统 CJK 字体，Qt 默认 Sans Serif 不包含中文字形。项目现将未修改的 WenQuanYi Micro Hei 0.2.0-beta 作为 Qt Resource 内嵌，在创建 `VirtualPlcWindow` 前设置全局字体；字体 SHA-256、上游版权和 Apache 2.0 许可证已随源码及安装文档保存。

范围说明：第一周验收时 Worker 只读取一次；第二周现已补齐周期轮询、单请求在途、通信线程、自动重连和数据流水线。报警、SQLite、历史查询和完整 UI 仍按第三周及后续周次推进。

### 第二周：通信稳定性与数据流水线

实施状态（2026-08-24，随实现进度更新）：

- [x] 协议 DTO：新增 Reconnecting、结构化错误、轮询/重连配置和实时快照元类型；`unit.protocol_types` 已验证。
- [x] DataPipeline、质量状态和最近 60 秒滚动统计；`unit.rolling_statistics` 与 `unit.data_pipeline` 已验证。
- [x] 通信 Worker 的周期轮询、单请求在途、状态机和自动重连；不响应端点与断线恢复场景由 `integration.modbus_tcp_worker` 验证。
- [x] DeviceSession 通信线程、断线恢复集成测试和退出无线程遗留验证；同一会话连续两轮停服恢复通过，关键线程/重连测试重复 10 轮通过。

- 完成通信线程、单请求在途和结构化错误。
- 完成状态机、超时、断线和自动重连。
- 完成 DataPipeline、质量状态和滚动统计。
- 完成对应单元与集成测试。

第二周结束标准：反复关闭和恢复 VirtualPLC 时能自动恢复采集，线程不泄漏。

第二周验证记录（2026-08-24）：

```text
cmake -S . -B build -DBUILD_TESTING=ON       -> 成功
cmake --build build -j2                      -> 成功
ctest --test-dir build --output-on-failure   -> 12/12 通过，0 失败
关键线程与重连测试 --repeat until-fail:10    -> 3 个测试各连续 10 轮通过
industrial_monitor --duration-ms 1200        -> 周期统计输出，Stopping -> Stopped，退出码 0
```

集成测试在允许监听本地环回端口的环境执行；受限沙箱内仍会因为不能申请 TCP 端口而失败，这不是产品回归。重复测试证明本次受测路径能够停止线程并恢复采集，不把 10 轮结果外推为生产级长稳结论。

### 第三周：报警、SQLite 与主要界面

- 完成报警生命周期、回差和确认。
- 完成 SQLite schema、批量写入和历史查询线程。
- 完成实时监控、设备管理和报警中心。
- 完成高温和通信故障演示链。

第三周结束标准：实时值、曲线、报警和数据库形成闭环。

### 第四周：历史、导出与工程化

- 完成历史曲线、分页查询和 CSV 导出。
- 完成通信日志页和滚动文件日志。
- 补齐异常测试、30 分钟重连测试和桌面布局检查。
- 编写 README、架构图、演示步骤和面试说明。
- 执行长时间稳定性测试并记录真实结果。

第四周结束标准：可以从干净构建开始，在 15 分钟内完成完整演示。

## 21. 首版验收标准

1. 主程序能够从 `plugins/` 动态发现并加载 Modbus TCP 插件。
2. 插件缺失或版本错误时主程序仍可启动，并明确展示加载错误。
3. VirtualPLC 在 `127.0.0.1:1502` 提供 Unit ID 1 的寄存器数据。
4. 上位机每 500 ms 发起采集，正常情况下持续显示五项实时数据。
5. 统计值明确表示最近 60 秒窗口，暂停显示不停止采集、报警和存储。
6. 写入目标转速成功后，VirtualPLC 的实际转速逐步变化。
7. 温度保持 86.3 ℃ 后产生一条 Critical 报警，恢复后更新同一报警记录。
8. 关闭 VirtualPLC 后产生一次通信报警并自动重连，恢复服务器后自动恢复采集。
9. 测量和报警数据正确写入 SQLite，数据库操作不阻塞 UI。
10. 可以查询最近一小时历史数据并成功导出 UTF-8 CSV。
11. 所有 Worker 和数据库连接均在所属线程创建和销毁，退出时无线程遗留。
12. 单元测试和集成测试由 CTest 统一执行，核心状态机、报警和寄存器编解码均有覆盖。
13. README 中的功能描述与实际实现和测试结果一致，不写未经验证的性能结论。

## 22. 演示脚本

完整演示控制在 10 至 15 分钟：

1. 启动 VirtualPLC，展示地址、端口和五个寄存器值。
2. 启动 IndustrialMonitor，展示从插件目录发现 Modbus TCP 驱动。
3. 连接设备，展示状态转移和实时曲线。
4. 修改目标转速，展示功能码 06 写入和模拟转速变化。
5. 制造高温，展示防抖后报警、确认和恢复。
6. 关闭 VirtualPLC，展示超时、通信报警和退避重连。
7. 恢复 VirtualPLC，展示自动恢复采集和通信报警恢复。
8. 查询刚才的历史数据和报警记录，导出 CSV。
9. 最后打开通信日志，解释请求地址、功能码、耗时和错误。

## 23. 面试说明边界

可以准确说明：

- 使用 Qt 动态插件隔离协议实现，并处理接口版本和插件生命周期。
- 使用 QObject Worker + QThread 保证 Modbus、计时器和 SQLite 的线程亲和性。
- 使用异步信号表达连接、读写和错误结果，而不是用同步 `bool` 假装操作已经完成。
- 使用单请求在途、超时、失败阈值和退避重连处理设备掉线。
- 使用数据质量、连续样本和回差减少误报警与报警抖动。
- 使用 SQLite WAL、独立读写连接和事务批处理保存历史数据。
- 使用 VirtualPLC 和集成测试验证采集、写入、报警、掉线和恢复的完整链路。

不能夸大为：

- 已适配真实 PLC 厂商或复杂工业现场。
- 支持全部 Modbus 功能码和 RTU。
- 实现了原始网络抓包器。
- 经过生产级高可用或大规模设备压力验证。

项目价值来自可运行的闭环、清晰的所有权和可复现的异常测试，而不是功能数量。

## 24. 参考资料

- Qt Modbus Client：<https://doc.qt.io/qt-6/qmodbusclient.html>
- Qt Modbus Client Example：<https://doc.qt.io/qt-6/qtserialbus-modbus-client-example.html>
- Qt Plugin How-To：<https://doc.qt.io/qt-6/plugins-howto.html>
- QPluginLoader：<https://doc.qt.io/qt-6/qpluginloader.html>
- QThread Worker 模式：<https://doc.qt.io/qt-6/qthread.html>
- Threads and QObjects：<https://doc.qt.io/qt-6/threads-qobject.html>
- QSqlDatabase：<https://doc.qt.io/qt-6/qsqldatabase.html>
- Qt Charts：<https://doc.qt.io/qt-6/qtcharts-index.html>
